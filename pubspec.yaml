name: getx_hive_flutter
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  get: ^4.6.1
  hive:
  hive_flutter:

  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  hive_generator:
  build_runner:

  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true
